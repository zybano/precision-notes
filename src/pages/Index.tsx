
import { Navigate } from "react-router-dom";

const Index = () => {
  // Redirect to B2B organizational documentation page
  return <Navigate to="/organizational-documentation" replace />;
};

export default Index;
